import { HapticClickDirective } from './haptic-click.directive';

describe('HapticClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HapticClickDirective();
    expect(directive).toBeTruthy();
  });
});
